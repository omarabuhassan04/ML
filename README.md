# ML
A Machine Learning project predicting cardiovascular disease using the CARDIO dataset. Models (Logistic Regression, Random Forest, Gradient Boosting, SVM) are tuned and compared using Accuracy, F1, AUC-ROC, and Confusion Matrix across train, validation, and test sets.
